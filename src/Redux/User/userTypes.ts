export interface User {
    email: string
    first_name: string
    job?: string
    last_name: string
    mobile: string
    permissions?: string[]
    phone: string
    role?: string
    avatar?: string
}
